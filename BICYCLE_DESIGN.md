# 自行车结构与运动约定

本项目使用独立的城市自行车模型 `createCityBike()`，不复用旧版自行车的坐标、曲柄相位或驱动侧。

## 几何尺寸

- 角色模型实测高度约 `1.706 m`，轮组半径 `0.345 m`，轴距 `1.46 m`。
- 座垫安装高度约 `1.06 m`，配合低跨城市车架，减少裙摆跨车架时的遮挡。
- 曲柄长度 `0.17 m`；牙盘等效半径 `0.13 m`，飞轮等效半径 `0.051 m`。
- 前轮位于本地 `+Z`，后轮位于本地 `-Z`；骑手右侧为本地 `-X`，驱动链条和牙盘均位于右侧。

## 车体结构

- 主车架由下管、座管、低跨双上管和下叉组成。
- 前叉、前轮、车把、车筐和车铃共同安装在独立转向总成上。
- 后轮包含轮毂、飞轮组和后碟刹。
- 牙盘与左右曲柄位于同一个 `crank-and-chainring` 旋转总成中。
- 踏板本体独立保持水平，只跟随曲柄销的位置移动。

## 传动和转向

- 车轮角位移：`Δθwheel = Δs / wheelRadius`。
- 曲柄角位移：`Δθcrank = Δθwheel × 18 / 46`。
- 骑行前进时车轮和曲柄都绕本地 `+X` 正向旋转，左右曲柄相位差固定为 `π`。
- 链条线速度由牙盘角速度和牙盘半径共同决定。
- `A` 令前轮向左、航向增大；`D` 令前轮向右、航向减小。
- 倒车时 `Δs < 0`，车轮与后轮传动自然反向；曲柄脱开驱动力并由踩地动作保持。

## 停车与裙摆

- 横向重心偏置决定支撑侧：重心偏左使用左腿，重心偏右使用右腿。
- 换边不会直接跳换，而是先保持双脚支撑约 `0.58 s`，再将身体重心移向目标脚。
- 非支撑脚继续处于曲柄踏板附近；支撑脚落在踏板上方、车架外侧的地面目标点。
- 裙摆和飘带骨骼根据速度、转向、倒车与落脚状态产生有限幅摆动，避免作为完全刚性服饰穿过座垫和腿。
